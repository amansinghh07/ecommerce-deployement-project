spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/amansinghh07/ecommerce-config-server
          username: ${GIT_USERNAME}
          password: ${GIT_TOKEN}
          default-label: master

server:
  port: 8888